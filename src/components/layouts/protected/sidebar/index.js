import React from "react";

const Sidebar = () => (
  <section className="sidebar-section">
    <nav className="section-nav">
      <ul className="nav-list">
        <li className="list-item">Home</li>
        <li className="list-item">Bookmark</li>
      </ul>
    </nav>
  </section>
);

export default Sidebar;
